fn scan(raw: String) -> Vec<String> {

    let mut processed_strings = Vec::new();
    let mut buffer_string = String::new();

    for c in raw.chars() {
        match c {
            ';' => {},
        }
    }
}
